dbt-core==1.11.12
dbt-duckdb==1.10.1
shandy-sqlfmt==0.30.0
black==24.10.0
# Transitive dependencies are omitted for simplicity. If you prefer a full freeze, you can run: pip freeze > requirements.txt

